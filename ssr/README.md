# SSR Simple Project

Minimal server-side rendered frontend using Express and EJS.

## Structure

- `server.js` - express server and route handler
- `views/index.ejs` - server-rendered template
- `public/styles.css` - static styles

## Start frontend

```bash
npm install
npm start
```

Then open `http://localhost:3004`.
