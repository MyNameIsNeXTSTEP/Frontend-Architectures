# MVVM Simple Project

Minimal frontend counter implemented with the MVVM pattern.

## Structure

- `public/app.js` - model, view model, and view bindings
- `public/index.html` - UI template
- `server.js` - tiny static file server

## Start frontend

```bash
npm install
npm start
```

Then open `http://localhost:3001`.
